[syntribos]
endpoint=<endpoint_to_be_tested>
templates=<directory of templates>
payload_dir=<directory of payload strings>

[vAPI]
url=<endpoint_to_get_token>
username=<username, if needed>
password=<password, if needed>
